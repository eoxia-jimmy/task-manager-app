import { FlextextDirective } from './flextext.directive';

describe('FlextextDirective', () => {
  it('should create an instance', () => {
    const directive = new FlextextDirective();
    expect(directive).toBeTruthy();
  });
});
