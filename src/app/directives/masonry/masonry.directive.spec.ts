import { MasonryDirective } from './masonry.directive';

describe('MasonryDirective', () => {
  it('should create an instance', () => {
    const directive = new MasonryDirective();
    expect(directive).toBeTruthy();
  });
});
