import { WpeoModalDirective } from './wpeo-modal.directive';

describe('WpeoModalDirective', () => {
  it('should create an instance', () => {
    const directive = new WpeoModalDirective();
    expect(directive).toBeTruthy();
  });
});
