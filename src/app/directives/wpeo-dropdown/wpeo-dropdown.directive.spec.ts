import { WpeoDropdownDirective } from './wpeo-dropdown.directive';

describe('WpeoDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new WpeoDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
